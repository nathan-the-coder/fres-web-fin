[project]
name = "fres-web-fin"
version = "2.0.0"
description = "FRES Web Portal - Filipino Reviewer & Evaluation System"
requires-python = ">=3.8"

[tool.vercel]
build-command = "pip install -r requirements.txt"
output-directory = "."
